{
  "name": "axon",
  "version": "0.2.9",
  "description": "Axon is a local agent workspace powered by Claude Code and a selected Ollama model",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node src/selfcheck.js",
    "dist:win": "electron-builder --win nsis"
  },
  "build": {
    "appId": "com.iyad.axion",
    "productName": "Axon",
    "asar": true,
    "files": [
      "src/**",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "src/assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "artifactName": "Axon-Setup-${version}.${ext}"
    }
  },
  "devDependencies": {
    "electron": "^36.0.0",
    "electron-builder": "^26.15.3"
  }
}
